# model-compression

> Model optimization and compression

## Usage

```javascript
import { model-compression } from '@ai-infra/model-compression';

const service = new model-compression();
await service.run();
```
